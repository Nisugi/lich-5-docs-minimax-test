# Python dependencies for Lich5 Documentation Automation

# Google Gemini SDK
google-generativeai>=0.3.0

# OpenAI SDK (optional - only if using OpenAI)
openai>=1.0.0

# Anthropic SDK (optional - only if using Claude)
anthropic>=0.18.0

# MiniMax-M2 Local (optional - only if using MiniMax-M2 via Ollama)
# Note: MiniMax-M2 requires Ollama to be installed and running locally
# No Python package needed - uses requests to communicate with Ollama HTTP API

# Utilities
python-dotenv>=1.0.0  # For loading .env files
pyyaml>=6.0          # For YAML configuration
requests>=2.31.0     # For API calls, GitHub integration, and Ollama communication

# Development/Testing
pytest>=7.4.0        # For running tests
black>=23.0.0        # Code formatting (optional)
flake8>=6.0.0        # Linting (optional)
